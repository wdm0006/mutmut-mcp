# Repo policy for obvious workers — auto-detected from GitHub settings (2026-09-06).
# defaultBranch: main (GitHub defaultBranchRef.name).
# mergeMethod: merge — all three methods are allowed on the repo and history shows
#   "Merge pull request #N" merge commits.
defaultBranch: main
mergeMethod: merge
requireHumanMerge: true
